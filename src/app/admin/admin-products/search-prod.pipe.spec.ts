import { SearchProdPipe } from './search-prod.pipe';

describe('SearchProdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProdPipe();
    expect(pipe).toBeTruthy();
  });
});
